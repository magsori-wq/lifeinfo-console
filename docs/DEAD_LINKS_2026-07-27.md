# 죽은 링크 목록 — 2026-07-27 실측 (정정판)

`tools/link_rot.py` 첫 전수 검사 결과. **본문 수정이 필요한 항목만** 모았다.
고친 항목은 체크하고, 전부 끝나면 이 문서를 지운다.

검사 방법: 공식 링크는 PC(한국 IP)에서 `--only official`, 그 외는 GitHub Actions.
왜 나누는지는 `lifeinfo-source/CLAUDE.md` 의 "공식 링크 부패 점검" 참조.

> ## 🔴 2026-07-27 13:4x 정정 — 초판의 "즉시 수정 6건" 중 **4건은 오탐이었다**
> 초판은 검사기의 **HEAD 404 오탐**(`4f63d1b` 에서 수정)을 그대로 옮겼다. `probe()` 의
> GET 재시도 조건이 `(403,405,400,501,500)` 이라 **404 는 GET 재확인 없이 죽음으로 확정**됐는데,
> `law.go.kr` · `nid.or.kr` · `newspim.com` 은 **HEAD 에 404, GET 에 200** 을 준다.
> 초판대로 본문을 고쳤다면 **멀쩡히 살아 있는 근거 링크 4개를 지울 뻔했다.**
> 아래는 GET 으로 하나하나 다시 확인한 결과다(한국 IP, 2026-07-27).
>
> | 코드 | 링크 | HEAD | GET | 판정 |
> |---|---|---|---|---|
> | F3-07 | `law.go.kr/…admRulSeq=2100000247532` | 404 | **200** | ❌오탐 (제목 "행정규칙 > 항공교통이용자 보호기준" 정상) |
> | D1-03 | `law.go.kr/lsInfoP.do?lsId=009620` | 404 | **200** | ❌오탐 |
> | C2-05 | `nid.or.kr` | 404 | **200** | ❌오탐 ("중앙치매센터" 정상) |
> | B1-03 | `newspim.com/news/view/20260701001458` | 404 | **200** | ❌오탐 (기사 살아있음) |
> | C2-03 | `kdca.go.kr/contents.es?mid=a20205050100` | 404 | 404 | ✅진짜 죽음 |
> | H1-01 | `hyundai.com/kr/ko/brand-story` | 404 | 404 | ✅진짜 죽음 |
> | E2-07 | `ko.wikipedia.org/wiki/대한민국의_FIFA_월드컵_중계` | 404 | 404 | ✅진짜 죽음 (**초판 누락** — 한글 URL 크래시에 가려져 있었다) |
>
> 📌 **교훈**: HEAD 응답만으로 링크 사망을 선고하지 말 것. 특히 국내 사이트는 HEAD 를
> 제대로 구현하지 않은 곳이 많다. 라이브 본문을 고치기 전에는 반드시 GET 으로 눈으로 확인한다.

---

## ⛔ 즉시 수정 — 진짜 죽은 링크 3건

- [ ] **C2-03** 폭염 온열질환
  `https://www.kdca.go.kr/contents.es?mid=a20205050100`
  → GET 도 404("알림메세지" 페이지). 질병관리청 사이트 개편으로 `mid` 무효.
  온열질환 감시체계 현행 페이지로 교체.

- [ ] **H1-01** 현대차 보스턴다이내믹스
  `https://www.hyundai.com/kr/ko/brand-story`
  → GET 도 404("Not found | 현대자동차"). 기업 페이지 개편.
  **링크 삭제를 권한다**(본문 근거로 꼭 필요한 링크가 아니다).

- [ ] **E2-07** 2026 월드컵 16강 8강 일정 무료 중계
  `https://ko.wikipedia.org/wiki/대한민국의_FIFA_월드컵_중계`
  → GET 404. 위키백과 API 로도 확인(`missing=true`) — **문서가 존재하지 않는다.**
  초판에는 없었다(한글 URL 크래시로 '확인 실패' 처리되던 것이 검사기 수정 후 드러남).

## ⚠️ 확인 필요 1건

- [ ] **D1-03** 학교폭력 신고 — `https://www.117.go.kr`
  → **DNS 미해석**(`getaddrinfo failed`). 링크 부재인지 국내 전용 DNS 문제인지
  브라우저로 직접 확인이 필요하다. 살아 있으면 손대지 말 것.
  ※ 같은 글의 `law.go.kr/lsInfoP.do?lsId=009620` 은 **정상이다**(초판의 오탐).

---

## 조치 불필요 (참고용)

고치지 말 것. 브라우저로는 정상 동작한다.

**🚧 차단 6건** — 봇 차단·레이트리밋
`busan.go.kr` 계열 4건(401) · `haeundae.go.kr`(401) · `idolbom.go.kr`(429)
> ✅ 429 원인이던 동시요청 문제는 **`4f63d1b` 에서 호스트별 1.5초 간격으로 해결**했다
> (네이버 OSMU 72건·블로그 내부 5건이 함께 해소). 다음 회차에는 401 만 남을 것이다.

**🔐 SSL 협상 실패 7건** — 서버 쪽 구형 설정
`etax.seoul.go.kr`(DH 키 too small) · `nip.kdca.go.kr`(레거시 재협상) ·
`bustago.or.kr` 2건(SSLV3 핸드셰이크) · `kca.go.kr` 3건(중간 인증서 누락)
> ✅ **`4f63d1b` 에서 SSL 오류 시에만 완화 컨텍스트로 재시도**하도록 했다. 위 5개 호스트는
> 전부 **200** 으로 확인됐고 결과에 "구형 TLS 로만 접속됨" 메모가 붙는다. 링크는 멀쩡하다.

**❓ 확인 실패 6건** — 타임아웃(한국에서도 느린 곳)
`hrd.go.kr` · `kinfa.or.kr` 2건 · `knia.or.kr` · `kua.go.kr`
> `kamis.or.kr`(406)은 차단으로 재분류됨.

**🔀 이전 23건** — 리다이렉트. 동작하므로 급하지 않다.
정리하려면 최종 주소로 갱신: `bokjiro` · `nps.or.kr` · `fss.or.kr` · `work24.go.kr` 등.

---

## 검사 요약 — 수정 후 전수 재검사 완료 (2026-07-27 13:45~13:5x, PC/한국 IP)

`4f63d1b` 로 오탐 3종을 고친 뒤 **글 153편 · 고유 링크 800개를 전수 재검사**한 확정 수치.
(PC 는 프록시 제약이 없어 공식·외부·내부·자산을 한 번에 볼 수 있다. `--only` 분리는 Actions 용.)

| 구분 | 1회차(버그 있는 상태) | **2회차(확정)** | 해석 |
|---|---|---|---|
| 정상 | 693 | **786** | +93 — 오탐이 정상으로 돌아왔다 |
| 이전(리다이렉트) | 33 | **39** | 전엔 못 보던 링크가 판정을 받았다 |
| **죽음** | 6 | **3** | 오탐 4건 제거, 위키백과 1건 추가 |
| 차단 | 72 | **3** | 네이버 OSMU 69건 전량 해소(호스트 간격) |
| 서버오류 | 5 | **0** | 블로그 내부링크 503 해소 |
| SSL 실패 | 7 | **0** | 완화 TLS 재시도로 전부 정상 판정 |
| 확인 실패 | 24 | **8** | 한글 URL·TLS 분 해소 |
| 고유 링크 총계 | 800 | 800 | 변동 없음 |

**죽은 링크 3건은 위 "즉시 수정" 목록과 정확히 일치**한다(C2-03 · H1-01 · E2-07).

### 남은 차단 3건 (조치 불필요)
`idolbom.go.kr`(429) · `koreanair.com` 2건(403) — 봇 차단. 부산시 계열 401 은 이번 회차에 정상으로 잡혔다.

### 남은 확인 실패 8건 (조치 불필요, 대부분 느린 정부 사이트)
`work24.go.kr` · `work.go.kr` · `knia.or.kr` · `kinfa.or.kr` 2건 — **20초 타임아웃 초과**(살아 있으나 느림).
`cyberts.kr`(연결 끊김) · `help.instagram.com`(400) · **`117.go.kr`(DNS 미해석 — 위 "확인 필요" 항목)**.

### 미참조 썸네일 2개 (전수 검사 기준 — 삭제해도 발행글 이미지 안 깨짐)
`128_visitjapanweb.png` · `15_property.png`
